import { View, Text, TouchableOpacity } from "react-native";
import React from "react";
import {
  createStaticNavigation,
  useNavigation,
} from "@react-navigation/native";

import { createNativeStackNavigator } from "@react-navigation/native-stack";

type RootStackParamList = {
  following: undefined;
  forYou: undefined;
};



export default function<RootStackParamList>() {

  const navigation = useNavigation();
  

  return (

<View className="flex flex-row justify-around border-b-2 border-neutral-800">
      <TouchableOpacity className="" onPress={() => navigation.navigate('following')}>
        <Text className="text-white text-xl">For You</Text>
      </TouchableOpacity>

      <TouchableOpacity className="">
        <Text className="text-white text-xl">Following</Text>
      </TouchableOpacity>
    </View>


  );
}


<View className='flex-1'>

{/* <Stack
  screenOptions={{
    headerShown: false,
    contentStyle: {
      backgroundColor: 'transparent', // Ensure Stack's content doesn't override the parent View
    },
  }}
>
  <Stack.Screen name="index" />
  <Stack.Screen name="following" />
</Stack> */}
{/* <NavigationContainer>
  <AppNavigator />
</NavigationContainer> */}

</View>
